<!DOCTYPE html>
<html lang="en">
<head>
<%- include('../partials/head.ejs') %>
</head>
<body>
    <header>
        <%- include('../partials/header.ejs') %>
    </header>

    <h2 class="gal-edit-add-h2">Edit Cover Image</h2>
    
    <div class="container center-align" style="width: 70%; padding-bottom: 125px;">
        <!-- Cover Image -->
        <img class="edit-img" src="<%=album.coverImage%>" style="width: 70%">

        <!-- Edit Form -->
        <form action="/album/<%=album._id%>?_method=PUT" method="POST">
            <div class="input-field">
                <label>Update album Name (max 20 char.)</label>
                <input type="text" maxlength="20" name="albumName" value="<%=album.albumName%>">
            </div>

            <div class="input-field">
                <label>Update Cover Image Link (.jpg)</label>
                <input type="text" name="coverImage" value="<%=album.coverImage%>">
            </div>
            
            <div class="input-field">
                <label>Description (max 150 char.)</label>
                <textarea class="materialize-textarea" maxlength="100" name="albumDescription"><%=album.albumDescription%></textarea>
            </div>

            <input class="btn waves-light blue-grey darken-2" type="submit" value="Submit Edit">
        </form>
    </div>
</body>
</html>